export const SearchComponent = (props) =>
{
    return(
        <div className="title-block-item">
            <input
            className="profile-input border-radius"
            placeholder="Введите запрос"/>
        </div>
    )
}